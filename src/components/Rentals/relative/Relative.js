import style from './relative.module.css'
export const Relative = () => {
    return (
        <div className={style.relative}>
            {/* navlinks leading back to path */}
        </div>
    )
}