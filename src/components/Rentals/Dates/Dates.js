import style from './dates.module.css'
export const Dates = () => {
    return (
        <div className={style.dates}>
            {/* select calendar dates and add it to context */}
        </div>
    )
}