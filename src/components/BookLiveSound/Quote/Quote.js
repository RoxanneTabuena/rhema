import style from './quote.module.css'
export const Quote = () => {
    return (
        <div className={style.quote}>
            {/* contact: name, phone, email; 
            event: audience size ,date(s), start time, end time, 
            venue: name/descrioption, address; 
            audience size, event descriotion  */}
            {/* consent, not a robot */}
        </div>
    )
}